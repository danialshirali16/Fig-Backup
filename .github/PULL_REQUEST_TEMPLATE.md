<!-- Title: short imperative summary, e.g. "Fix queue clipping at small window heights" -->

## What does this PR change?

<!-- A sentence or two: the user-visible behavior and the approach taken. -->

## How was it tested?

<!-- e.g. `python -m unittest discover -s tests`, `node --test tests/download-errors.test.js`,
     manual run on macOS 15, UI preview at http://127.0.0.1:5173/?preview=1 -->

## Checklist

- [ ] Tests pass (`python -m unittest discover -s tests` and `node --test tests/download-errors.test.js`)
- [ ] New UI strings added to **all** language objects in `src/i18n.js`
- [ ] If `src/` changed: `dist/` rebuilt with `npm run build` and committed in this PR
- [ ] Docs updated if behavior changed (README, docs/ARCHITECTURE.md, docs/UI.md)
- [ ] No access tokens or personal data included
